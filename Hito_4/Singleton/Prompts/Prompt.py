# Crea 10 ejemplos distintos del patrón de diseño Singleton en Python.

# Cada ejemplo debe:
# - Implementar una clase que asegure que solo se puede instanciar una vez.
# - Puedes usar métodos como `__new__`, `metaclass`, variables de clase, decoradores, etc.
# - Muestra con un ejemplo de uso que múltiples llamadas a la clase devuelven la misma instancia.

# Varía la dificultad:
# - Empieza con ejemplos básicos usando `__new__` y variables estáticas.
# - Luego incluye otros métodos como `metaclass`, decoradores, y control de instanciación por archivo de configuración o inicialización diferida (lazy init).
# - Incluye ejemplos más avanzados como: singleton con parámetros, singleton por clase base, y al menos 2 ejemplos en múltiples archivos (por ejemplo, `singleton.py`, `logger.py`, `main.py`).

# Devuelve cada ejemplo claramente separado así:

# ### Ejemplo 4: Singleton con decorador

# #### singleton_decorator.py
# ```python
# # contenido
