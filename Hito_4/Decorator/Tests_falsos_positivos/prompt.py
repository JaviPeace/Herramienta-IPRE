# Crea 15 ejemplos de código en Python que parezcan utilizar el patrón de diseño Decorator pero que en realidad sean falsos positivos.

# Los ejemplos deben:

# Incluir clases llamadas Component, ConcreteComponent, Decorator, ConcreteDecorator o nombres similares para simular el patrón Decorator.

# Tener métodos con nombres típicos de Decorator como operation(), execute(), send(), etc.

# Simular la estructura de Decorator (por ejemplo, Decorator recibiendo un Component en el constructor) pero de forma incorrecta:

# El Decorator no llama al método del Component interno.

# El Decorator no mantiene referencia a Component.

# El Decorator modifica el comportamiento de manera incorrecta.

# Herencias mal implementadas o Decorator implementado como función en lugar de clase.

# Uso de nombres correctos pero sin estructura Decorator real (por ejemplo, múltiples Decorator pero no envuelven el Component).

# Variar en dificultad: algunos ejemplos simples, otros con múltiples métodos o jerarquías confusas.

# Los ejemplos deben ser sintácticamente correctos y ejecutables.

# Cada ejemplo de 10 a 30 líneas máximo.

# Entrega cada ejemplo con un título breve como:

# shell
# Copiar
# Editar
# ### Ejemplo 1: Decorator que no llama al componente
# seguido del bloque de código.

# No expliques el error; solo genera el código.

# No incluyas explicaciones fuera de los ejemplos.

# El objetivo es probar detectores de patrones de diseño para identificar falsos positivos de Decorator que parecen correctos superficialmente.

