# Crea 10 ejemplos distintos del patrón de diseño Decorator en Python.

# Cada ejemplo debe:
# - Incluir una clase base con un método (por ejemplo, `send()`, `draw()`, `cost()`, etc.).
# - Incluir una implementación concreta de esa clase base.
# - Incluir al menos un decorador que añade funcionalidad adicional al método de la clase base.
# - Reflejar el patrón Decorator de forma clara y válida.
# - Estar en Python puro, sin bibliotecas externas.

# Varía la dificultad:
# - Los primeros ejemplos deben ser simples (1 archivo, 1 decorador).
# - Aumenta la dificultad gradualmente: incluye decoradores anidados, múltiples decoradores, clases abstractas, y al menos dos ejemplos que estén divididos en múltiples archivos (por ejemplo, `component.py`, `decorators.py`, `main.py`).

# Devuelve cada ejemplo en bloques separados claramente (por ejemplo, con encabezados tipo `### Ejemplo 1: Decorator básico` y comentarios explicativos dentro del código).

# No incluyas texto fuera del código salvo encabezados y nombres de archivo si hay múltiples archivos.

# Formato ideal (cuando haya más de un archivo):

### Ejemplo 6: Decorador de notificaciones

#### component.py
# ```python
# contenido...