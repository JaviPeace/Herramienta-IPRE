# Crea 10 ejemplos distintos del patrón de diseño Observer en Python.

# Cada ejemplo debe:
# - Incluir una clase `Subject` (o equivalente) que mantiene una lista de observadores (listeners, subscribers, etc.).
# - Incluir una clase `Observer` o interfaz base con un método como `update()`.
# - La clase `Subject` debe permitir registrar observadores y notificarles cuando hay un cambio.
# - El patrón debe implementarse de forma explícita, sin usar bibliotecas externas.

# Varía la dificultad:
# - Comienza con ejemplos simples: subject que notifica observadores con un solo valor.
# - Aumenta progresivamente: múltiples tipos de eventos, múltiples observadores, suscripciones condicionales, priorización de eventos.
# - Crea al menos 2 ejemplos distribuidos en varios archivos (`subject.py`, `observer.py`, `main.py`, etc.).

# Organiza cada uno con este formato:

# ### Ejemplo 7: Sistema de monitoreo de temperatura

# #### subject.py
# ```python
# # contenido
