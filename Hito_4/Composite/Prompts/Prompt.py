# Crea 10 ejemplos distintos del patrón de diseño Composite en Python.

# Cada ejemplo debe:
# - Incluir una clase base (por ejemplo, `Component`) con uno o más métodos (`operation()`, `render()`, `cost()`, etc.).
# - Incluir al menos una subclase `Leaf` que implemente el método base.
# - Incluir una clase `Composite` que contenga una colección de componentes (hojas o composites anidados) y que implemente la operación de forma recursiva.
# - Reflejar el patrón Composite de forma clara.

# Varía la dificultad:
# - Los primeros ejemplos deben ser básicos (una estructura simple, 1 archivo).
# - Aumenta la dificultad gradualmente: por ejemplo, permite múltiples métodos, composiciones anidadas, árboles con diferentes tipos de nodos, y validaciones.
# - Incluye al menos 2 ejemplos que se dividan en múltiples archivos (por ejemplo, `component.py`, `leaf.py`, `composite.py`, `main.py`).

# Devuelve cada ejemplo en bloques separados claramente con encabezados tipo:

# ### Ejemplo 1: Composite básico

# #### composite
